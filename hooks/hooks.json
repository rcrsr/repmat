{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "npx -p @rcrsr/mcp-policy-server policy-fetch --hook --config \"{\\\"files\\\": [\\\"$CLAUDE_PROJECT_DIR/.claude/policies/*.md\\\", \\\"${CLAUDE_PLUGIN_ROOT}/policies/*.md\\\"]}\""
          }
        ]
      }
    ]
  }
}
